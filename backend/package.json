{
  "name": "video-call-app",
  "version": "1.0.0",
  "description": "Simple WebRTC Video Call App",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd ../frontend && npm install && npm run build && cd ../backend && node build.js",
    "copy": "node build.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
